apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: coder-tls
  namespace: coder
spec:
  secretName: coder-tls
  issuerRef:
    name: letsencrypt-nolimitz
    kind: ClusterIssuer
  dnsNames:
    - coder.nolimitz.io
    - "*.coder.nolimitz.io"
